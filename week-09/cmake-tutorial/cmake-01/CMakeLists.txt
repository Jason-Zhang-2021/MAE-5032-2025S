CMAKE_MINIMUM_REQUIRED(VERSION 3.10)
PROJECT(HELLO)
MESSAGE(STATUS "SRC_DIR = ${HELLO_SOURCE_DIR}")
MESSAGE(STATUS "BIN_DIR = ${HELLO_BINARY_DIR}")
MESSAGE(STATUS "BUILD_TYPE = ${CMAKE_BUILD_TYPE}")
SET(SRC main.c)
ADD_EXECUTABLE(hello ${SRC})
